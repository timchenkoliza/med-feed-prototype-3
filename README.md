# Doctor profile onboarding — comparison prototype

Second prototype for comparing onboarding approaches.

Flow: specialty → optional flat practice areas → experience → workplace → patient groups → preferred sources → summary.

Constraints: checkbox-only selectable controls, no chips/clouds/dropdowns; child/adult is not part of specialty taxonomy.
